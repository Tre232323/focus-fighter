import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tonnom.focusfighter',
  appName: 'focus fighter rpg',
  webDir: 'dist'
};

export default config;
